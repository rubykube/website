# Rubykube website

Rubykube website uses Middleman  

To run the project locally:  
```shell
bundle install
npm install
bundle exec middleman
```

To deploy

```shell
make deploy
```

Other commands:

```shell
make help
```

